// import * as types from "./actionTypes";

const initialState = {};

export const reducer = (state = initialState) => {
  // const { type, payload } = action;

  // switch (type) {
    // default:
      return state;
  // }
};
